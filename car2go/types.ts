type Vehicles = {
  id?: string,
  image?: string,
  description?: string,
  title?: string,
  category?: string,
  header?: string,
}

type Categories = {
  title?: string,
  id?: string;
  image?:string,
}